{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "module": "NodeNext",
        "moduleResolution": "NodeNext",

        /* The only strategy that emits: `test` executes dist/node/smoke.js. */
        "outDir": "./dist/node",
        "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.nodenext.tsbuildinfo"
    }
}
